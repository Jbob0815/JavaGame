package game.entity;

public class EnemyProjectile extends Bullet {
    public EnemyProjectile(float startX, float startY, float dirX, float dirY) {
        super(startX, startY, dirX, dirY);
    }
}
